[project]
name = "transaction-engine"
version = "0.1.0"
description = "Core transaction classification and sanitization logic."
readme = "README.md"
authors = [
    { name = "Lucas Paes", email = "lucas.paes@motion.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
