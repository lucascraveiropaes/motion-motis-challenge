[project]
name = "finguard-workspace"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = ["application-agents/*", "packages/*"]

[tool.ruff]
line-length = 120
[tool.ruff.lint]
select = ["F", "I", "ARG"]
